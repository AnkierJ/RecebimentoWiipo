# Copie este arquivo para .streamlit/secrets.toml (local) ou cole o
# conteúdo em Settings > Secrets no Streamlit Community Cloud.
# NUNCA commite o secrets.toml real.

supabase_url = "https://xxxxxxxx.supabase.co"
supabase_service_key = "eyJhbGciOi..."   # service_role key (Supabase > Project Settings > API)

admin_password = "troque-esta-senha"

# URL pública onde o app fica publicado, usada para montar os links dos
# gestores (ex: https://wiipo-recebidos.streamlit.app)
app_base_url = "https://SEU-APP.streamlit.app"
